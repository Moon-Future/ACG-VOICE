export const playMode = {
  sequence: 0,
  loop: 1,
  random: 2,
  one: 3
}

export const playModeList = [
  playMode.sequence,
  playMode.loop,
  playMode.random,
  playMode.one
]

export const code = {
  success: 200
}

export const toastMessage = {
  noCopyright: '没有版权'
}